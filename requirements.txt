pandas>=2.0
numpy>=1.24
matplotlib>=3.7
folium>=0.15         # interactive maps
branca>=0.7          # colormaps (installed with folium)
# All data APIs used are free, government-published endpoints — no paid keys required.
# Optional: set CENSUS_API_KEY env var for Census ACS (free key at api.census.gov/key_signup.html)
